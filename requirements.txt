# Core (already present in the target env, pinned for reproducibility)
sentence-transformers>=3.0
torch>=2.0
numpy>=1.24

# Added by this harness
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
jinja2>=3.1

# LiteRT/TFLite backend (Gecko on-device models): tf.lite.Interpreter runtime,
# SentencePiece tokenizer, and HuggingFace Hub for lazy model download.
tensorflow>=2.16
sentencepiece>=0.2
huggingface_hub>=0.23
